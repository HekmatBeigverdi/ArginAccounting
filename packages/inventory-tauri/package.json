{
  "name": "@argin/inventory-tauri",
  "version": "0.20.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"Inventory Tauri lint is not configured yet\"",
    "test": "node --experimental-strip-types --experimental-transform-types --test tests/*.test.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@argin/audit": "workspace:*",
    "@argin/database": "workspace:*",
    "@argin/inventory": "workspace:*",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "rimraf": "^6.0.0",
    "typescript": "^5.9.0"
  }
}
