{
  "name": "@argin/company",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"Company lint is not configured yet\"",
    "test": "echo \"Company tests are not configured yet\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@argin/core": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.9.0"
  }
}
