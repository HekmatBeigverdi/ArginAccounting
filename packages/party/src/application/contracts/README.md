# Party Application Contracts

This directory defines persistence-neutral commands, queries, DTOs, reader/repository contracts, Unit of Work boundaries, and stable application errors for the Party bounded context.

It intentionally contains no SQLite, PostgreSQL, HTTP, Tauri, React, authorization implementation, duplicate-detection implementation, or synchronization transport logic.
