{
  "name": "@argin/product-tauri",
  "version": "0.18.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"Product Tauri lint is not configured yet\"",
    "test": "node --experimental-strip-types --experimental-transform-types --test tests/*.test.ts",
    "validate:performance": "node --experimental-strip-types --experimental-transform-types scripts/validate-product-performance.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@argin/database": "workspace:*",
    "@argin/product": "workspace:*",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "rimraf": "^6.0.0",
    "typescript": "^5.9.0"
  }
}
