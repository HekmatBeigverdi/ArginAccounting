# Disable pnpm update notifications
update-notifier=false

# Optional: Disable update checks entirely
update-check=false

# Disable supply-chain security checks temporarily
resolution-policy-allow-blocked-versions=true
